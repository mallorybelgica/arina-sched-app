import React from "react";
import styled from "styled-components";

const Analytics = () => {
  return <Wrapper>ANALYTICS</Wrapper>;
};

export default Analytics;

const Wrapper = styled.div`
  font-family: "Montserrat", sans-serif;
  text-align: center;
  font-size: 56px;
`;
