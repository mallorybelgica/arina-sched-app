import React from "react";
import styled from "styled-components";

const UserAccount = () => {
  return <Wrapper>USER ACCOUNT</Wrapper>;
};

export default UserAccount;

const Wrapper = styled.div`
  font-family: "Montserrat", sans-serif;
  text-align: center;
  font-size: 56px;
`;
